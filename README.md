# ie2102-mini-project
TCP Network Programming mini project demonstrating client-server communication using sockets in C, including iterative and concurrent server models with fork(), process management, and signal handling (SIGCHLD, waitpid) to support multiple clients.
